; [global] (user_agent dahil) artık portal tarafından üretilir:
; pbx/pjsip_transports.conf (SyncTransports.php) — Santral Ayarları'ndaki
; "SIP User Agent" alanından yönetiliyor (2026-08-31). Buraya TEKRAR
; [global] yazılmamalı, mükerrer tanım config yükleme hatasına yol açar.
;
; Transportlar da aynı şekilde portal tarafından üretilir.

#include "pbx/pjsip_*.conf"
