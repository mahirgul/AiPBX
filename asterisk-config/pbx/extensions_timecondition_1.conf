; Native Dialplan Time Condition ID 1: CM Mesai (Auto-generated by AI PBX)
[app-timecondition-1]
exten => s,1,NoOp(Evaluating Native Time Condition ID 1: CM Mesai)
 same => n,GotoIfTime(08:30-12:30,mon-fri,*,*?tc_rule_0)
 same => n,GotoIfTime(13:30-23:00,mon-fri,*,*?tc_rule_1)
 same => n,Goto(tc_nomatch)

 same => n(tc_rule_0),NoOp(Time Condition 1 Rule 1 Matched -> ivr)
 same => n,Goto(app-ivr-1,s,1)

 same => n(tc_rule_1),NoOp(Time Condition 1 Rule 2 Matched -> ivr)
 same => n,Goto(app-ivr-1,s,1)

 same => n(tc_nomatch),NoOp(Time Condition 1 Not Matched -> hangup)
 same => n,Playback(custom/CM_MDISI)
 same => n,Hangup()

 same => n,Hangup()
