; Geolocation module is not used on this system (no endpoint references a
; geoloc profile). Left intentionally empty to avoid the sample/doc content
; being mis-parsed as live config. Original moved to
; /root/backups/config_cleanup_20260817/geolocation.conf.orig
[general]
